# ตรวจสุขภาพแอป HICHAO.CNX — 1 ส.ค. 2569

ตรวจจากของจริงบน production (`hichao-rental.vercel.app`) ไม่ใช่การเดาจากโค้ด
ทุกตัวเลขในเอกสารนี้วัดมาจริง

---

## สรุปภาพรวม

ระบบ **แข็งแรงกว่าที่คิด** — เปิดแอป 1 ครั้งยิงแค่ 4 requests รวม 73 KB
โหลดเสร็จใน ~340 ms ไม่มีการดึงข้อมูลซ้ำซ้อนใหญ่ๆ

ปัญหาที่เจอส่วนใหญ่ไม่ใช่เรื่อง "ช้า" หรือ "เปลือง" แต่เป็นเรื่อง
**ระบบพังแบบเงียบๆ แล้วไม่มีอะไรบอก** ซึ่งเพิ่งกัดเราไปหมาดๆ

---

## 1. เรียกข้อมูลตอนเปิดแอป (วัดจริง)

| Query | จำนวนแถว | ขนาด |
|---|---|---|
| `rentals` (+ join กล้อง/ลูกค้า) | 57 | **49.1 KB** |
| `customers` | 72 | 17.4 KB |
| `cameras` | 7 | 4.8 KB |
| `expenses` | 11 | 2.0 KB |
| **รวม** | | **73.4 KB** |

**ประเมิน:** ยังไม่เป็นปัญหาที่ขนาดนี้ แต่ `rentals` หนัก ~860 ไบต์/แถว
และเป็นตัวเดียวที่โตตามการใช้งาน — ถึง 500 รายการเมื่อไหร่จะกลายเป็น ~430 KB ต่อการเปิด 1 ครั้ง

### จุดที่กินเกินจำเป็น

| # | เรื่อง | ผลกระทบ |
|---|---|---|
| 1 | **realtime 1 event = ดึง `rentals` ใหม่ทั้งตาราง** แม้เปลี่ยนแค่แถวเดียว · pg_cron เปลี่ยนสถานะทุกนาที | 49 KB ต่อครั้ง × ทุกเครื่องที่เปิดค้างไว้ |
| 2 | **`ExpensesPage` ดึง `expenses` เองซ้ำ** ทั้งที่ `AppContext` โหลดไว้แล้ว | +1 query ทุกครั้งที่เข้าหน้าการเงิน และข้อมูลอาจไม่ตรงกับหน้าหลัก |
| 3 | คอลัมน์ที่ดึงมาแต่**ไม่ได้ใช้เลย**: `pickup_reminded`, `return_reminded`, `updated_at` | เล็กน้อย (~5%) |
| 4 | ไม่มี pagination ที่ไหนเลย | โตไม่มีเพดาน |

---

## 2. ปัญหาที่ควรแก้ (เรียงตามความเสี่ยง)

### 🔴 สูง — ไม่มี Error Boundary

JavaScript error จุดเดียวที่ไหนก็ได้ = **จอขาวทั้งแอป** ผู้ใช้ไม่เห็นอะไรเลย
ไม่มีข้อความ ไม่มีปุ่มลองใหม่ ต้องเดาเองว่าต้อง refresh

> ร้านที่ใช้งานจริงหน้าเคาน์เตอร์ อาการนี้แปลว่า "ทำงานต่อไม่ได้" ทันที

### 🔴 สูง — ระบบล้มแบบเงียบ ไม่มีอะไรฟ้อง

ตอนนี้ถ้าโหลดตารางใดไม่สำเร็จ โค้ดแค่ `console.error` แล้วไปต่อ
หน้าจอจะโชว์ข้อมูลไม่ครบ**โดยผู้ใช้ไม่รู้ตัว**

นี่คือสาเหตุที่บั๊ก `serial_number` หายากมาก — ทุกอย่างดู "ปกติ" แค่ว่างเปล่า
ควรมีแถบเตือนเวลาโหลดไม่ครบ แบบเดียวกับแถบ 402 ที่ทำไว้แล้ว

### 🟠 กลาง-สูง — สิทธิ์ในฐานข้อมูลหลวม

```sql
using (auth.role() = 'authenticated')
```

แปลว่า **ใครก็ตามที่ล็อกอินได้ อ่าน/แก้/ลบได้ทุกอย่าง** ไม่มีการแยกบทบาท

ตอนนี้มีผู้ใช้คนเดียวเลยไม่เป็นไร แต่วันที่รับพนักงานมาช่วยคีย์ข้อมูล
พนักงานจะลบรายการเช่าทั้งปีได้ในคลิกเดียว และไม่มีบันทึกว่าใครทำ

### 🟠 กลาง — `schema.sql` ไม่ตรงกับฐานข้อมูลจริง

`serial_number` มีในไฟล์แต่ไม่มีในตาราง (แปะหมายเหตุเตือนไว้แล้ว แต่ยังไม่ได้แก้ให้ตรงจริง)
ไฟล์ที่เชื่อไม่ได้อันตรายกว่าไม่มีไฟล์ เพราะคนอ่านจะเชื่อ

### 🟡 ต่ำ — anon key ฝังใน SQL

`migration_003.sql` / `migration_004.sql` ฝัง anon key ไว้ตรงๆ
เป็น public key จึงไม่ใช่ช่องโหว่ แต่ถ้าวันหนึ่ง rotate key ต้องไล่แก้หลายที่

### 🟡 ต่ำ — โค้ดตายค้างอยู่

| ไฟล์ | บรรทัด | สถานะ |
|---|---|---|
| `src/__selftest__.jsx` | 1 | ไฟล์ว่าง ไม่มีใครใช้ |
| `src/components/StatusBadge.jsx` | 18 | ไม่มีใคร import |
| `src/components/CalendarView.jsx` | 180 | ไม่มีใคร import — ชื่อไปชนกับตัวแปร `CalendarView` ใน `RentalsPage` เลยดูเหมือนถูกใช้ |

---

## 3. ข้อเสนอยกระดับให้ดูมืออาชีพ

เรียงตาม **ผลลัพธ์ที่รู้สึกได้ ต่อ แรงที่ลง**

### คุ้มที่สุด (ทำไม่นาน เห็นผลชัด)

| # | สิ่งที่ทำ | ได้อะไร |
|---|---|---|
| 1 | **Error Boundary + หน้า "เกิดข้อผิดพลาด"** พร้อมปุ่มลองใหม่ | เลิกจอขาว · ดูเป็นซอฟต์แวร์ที่ผ่านการคิดมา |
| 2 | **แถบเตือนเมื่อโหลดข้อมูลไม่ครบ** | เลิกเดาว่าข้อมูลหายไปไหน |
| 3 | **Optimistic update** — กดคืนกล้องแล้วอัปเดตในจอทันที ไม่ต้องรอ refetch ทั้งตาราง | รู้สึก "ไว" ขึ้นทันตา และประหยัด 49 KB ต่อการกด 1 ครั้ง |
| 4 | **ให้ `ExpensesPage` ใช้ข้อมูลจาก AppContext** | ตัด query ซ้ำ + ข้อมูลตรงกันทั้งแอป |
| 5 | **ลบโค้ดตาย 199 บรรทัด** | อ่านโค้ดง่ายขึ้น ลดโอกาสแก้ผิดไฟล์ |

### ระยะกลาง

| # | สิ่งที่ทำ | ได้อะไร |
|---|---|---|
| 6 | **Code splitting ต่อหน้า** (`React.lazy`) — ตอนนี้ bundle ก้อนเดียว 624 KB (gzip 168 KB) | เปิดครั้งแรกเหลือ ~200 KB · มือถือเน็ตช้าต่างกันชัด |
| 7 | **แยก `RentalsPage` (1,008 บรรทัด)** เป็นคอมโพเนนต์ย่อย | แก้อะไรก็ไม่กลัวพังจุดอื่น |
| 8 | **Pagination / โหลดเพิ่มเมื่อเลื่อน** ในรายการเช่าและลูกค้า | รองรับข้อมูลโตได้ไม่จำกัด |
| 9 | **Backup อัตโนมัติ** — ตอนนี้ต้องกดปุ่มเอง ถ้าลืมคือไม่มี | นอนหลับสบายขึ้น |
| 10 | **แยกบทบาท admin / staff** ใน RLS + ตาราง audit log | รับพนักงานได้โดยไม่ต้องเสี่ยง |

### ระยะยาว

| # | สิ่งที่ทำ | ได้อะไร |
|---|---|---|
| 11 | **Error tracking** (เช่น Sentry แผนฟรี) | รู้ว่าลูกค้าเจอ error อะไรโดยไม่ต้องรอโทรมาบอก |
| 12 | **เทสต์อัตโนมัติ** ของ logic การเงิน (คำนวณวัน/ยอด/มัดจำ) | ตอนนี้ยังไม่มีเทสต์เลยสักตัว · logic เงินผิดคือเสียเงินจริง |
| 13 | **PWA ใช้งานออฟไลน์ได้** | เน็ตร้านหลุดก็ยังดูคิววันนี้ได้ |
| 14 | **หน้าพิมพ์/ส่งออกรายงานเป็น PDF** | ใช้คุยกับบัญชี/ภาษีได้เลย |

---

## 4. สิ่งที่ตรวจแล้ว "ไม่มีปัญหา"

- ✅ `.env` ไม่ถูก commit ขึ้น git
- ✅ ทุกหน้าใช้ข้อมูลจาก `AppContext` (ยกเว้น `ExpensesPage` ตามข้อ 2)
- ✅ ไม่มีคำขอไป Supabase Storage อีกแล้ว — Cached Egress = 0
- ✅ โหลดหน้าเสร็จ ~340 ms
- ✅ ไม่มี import ค้าง / ตัวแปรใช้ก่อนประกาศ (ผ่าน `npm run check` ทุกครั้งก่อน deploy)
- ✅ realtime subscribe แค่ 2 ตารางที่จำเป็น และเปิดเฉพาะตอนล็อกอินแล้ว

---

## ถ้าให้เลือกทำแค่ 3 อย่าง

1. **Error Boundary** — กันจอขาว
2. **แถบเตือนตอนโหลดไม่ครบ** — เลิกพังเงียบ
3. **Optimistic update** — ไวขึ้นทันตา และลด egress ต่อการกด 1 ครั้ง

สามอย่างนี้แก้ "ความรู้สึกว่าแอปเชื่อถือได้" ได้มากที่สุด
โดยไม่ต้องรื้อโครงสร้างอะไรเลย
